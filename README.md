# Filter Products by Price+Discount+Rating

I have created a React app to filter proudct based on price, discount and rating. 

## React Concepts Used
To manage the filter state effectively, I have utilized the `useReducer` hook in React. 

This approach allows for a more organized and scalable way to handle complex state logic, especially when multiple filter criteria are involved.